<div class="mr10">
  <%= link_to like_path(current_user.likes.find_by(board_id: board.id)), method: :delete, id: "js-like-button-for-board-#{board.id}", remote: true do %>
    <div class="btn btn-primary">投票済み</div>
  <% end %>
</div>